{
  "setup-worktree-unix": [
    "test -f \"$ROOT_WORKTREE_PATH/.env\" && cp \"$ROOT_WORKTREE_PATH/.env\" .env || true",
    "test -d \"$ROOT_WORKTREE_PATH/data/vn2\" && mkdir -p data && cp -R \"$ROOT_WORKTREE_PATH/data/vn2\" data/vn2 || true",
    "test -d \"$ROOT_WORKTREE_PATH/data/m5\" && mkdir -p data && cmd //c mklink //J 'data\\m5' \"$(cygpath -w \"$ROOT_WORKTREE_PATH/data/m5\")\" || true",
    "uv sync --frozen --extra dev --extra benchmarks --extra hierarchy"
  ]
}
